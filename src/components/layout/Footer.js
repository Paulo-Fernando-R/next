import styles from '../../styles/layout/Footer.module.scss'
export default function Footer(){
    return(
        <footer className={styles.footer}>
            <p className={styles.text}>Principal</p>
        </footer>
    )
}